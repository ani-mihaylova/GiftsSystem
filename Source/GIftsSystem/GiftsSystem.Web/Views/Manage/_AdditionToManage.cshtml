@model GiftsSystem.Web.Models.IndexViewModel
<dl class="dl-horizontal">
    <dt>Picture</dt>
    <dd>
        @*<img src="@Model.ImagePath" />
        <label>Change image</label>
        <input type="image" />*@
    </dd>
    <dt>Wish list</dt>
    <dd>
        <div class="list-group">
            @if (Model.WishList != null)
            {
                @*@for (int i = 0; i < Model.WishList.Count; i++)
                    {
                        if (i == 0)
                        {
                            <a href="#" class="list-group-item active">
                                <h4 class="list-group-item-heading">@Model.WishList[i].Name</h4>
                                <p class="list-group-item-text">@Model.WishList[i].Description</p>
                            </a>
                        }
                        else
                        {
                            <a href="#" class="list-group-item">
                                <h4 class="list-group-item-heading">@Model.WishList[i].Name</h4>
                                <p class="list-group-item-text">@Model.WishList[i].Description</p>
                            </a>
                        }
                    }*@
            }
            @*@foreach (var item in Model.WishList)
            {
                <a href="#" class="list-group-item">
                    <h4 class="list-group-item-heading">@item.Name</h4>
                    <p class="list-group-item-text">@item.Description</p>
                </a>
            }*@
                      
        </div>
    </dd>
</dl>
